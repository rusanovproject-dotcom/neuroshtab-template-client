# Стратегия клиента — заполняется в /strategist-roadmap

Этот файл появляется автоматически после того как клиент пройдёт онбординг со Стратегом.

Если ты Director и читаешь это — значит онбординг ещё не пройден. Мягко предложи клиенту распаковку через `/strategist-intake`, но работай с его текущей задачей без блокировки. Базовый контекст о клиенте может быть в `office/client-profile.md` (заполняется в фазе PREPARE).
