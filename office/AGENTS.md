# Команда офиса

## Core Team (с 1 дня)

| Агент | Роль | Когда зовём |
|-------|------|-------------|
| **Director** | Оркестратор, Chief of Staff | Всегда начинаем отсюда — любая задача идёт через него |
| **Стратег** | Персональный партнёр 6-недельной программы | `/strategist-intake` на старте, `/strategist-checkin` еженедельно |
| **Демиург** | Архитектор офиса, создатель агентов | Когда нужно подключить/создать нового агента в команду |

## Подключаются по мере нужды (создаёт Демиург)

По мере роста проекта клиент просит подключить нужных агентов. Демиург создаёт их **персонально под клиента** (voiceprint, бренд, контекст), а не из шаблона.

| Роль | Когда подключаем | Триггер |
|------|------------------|---------|
| **Copywriter** | когда есть regular контент | "нужен копирайтер" / "/add-agent copywriter" |
| **Designer** | когда есть визуал / вёрстка | "нужен дизайнер" / "/add-agent designer" |
| **Producer** | когда нужна стратегия / офферы | "нужен продюсер" / "/add-agent producer" |
| **Hermes** | когда есть воронка продаж | "нужен агент продаж" / "/add-agent hermes" |
| **Tech Lead** | когда есть код / деплой | "нужен tech lead" / "/add-agent tech-lead" |
| **Intelligence** | когда нужна разведка рынка | "нужна разведка" / "/add-agent intelligence" |
| **Brahma** | когда нужен критик важных решений | "нужен ревьюер" / "/add-agent brahma" |
| **ПАРА** | когда бардак в inbox / файлах | "/add-agent para" |
| **Кастомный** | любая специфичная роль | "/add-agent <custom-name>" |

## Подключённые через Демиурга

*(таблица заполняется автоматически когда Демиург создаёт нового агента)*

| Имя | Роль | Создан | Зачем создали |
|-----|------|--------|---------------|
| — | — | — | — |

---

## Как работать с агентами

**Обычная задача** — через Director:
> "Нужно написать пост про запуск продукта"

Director сам позовёт нужного агента. Если этого агента ещё нет в команде — предложит подключить через Демиурга.

**Подключить нового агента:**
```
/add-agent <role>
```
или просто: "добавь копирайтера". Демиург получит контекст из `office/strategy/` и создаст персонального агента.

**Онбординг программы:**
```
/strategist-intake     # первичная распаковка
/strategist-unpack     # детальный разбор документов
/strategist-discovery  # уточняющий диалог
/strategist-roadmap    # финал: стратегия + 6-недельный план + HTML-роадмап
/strategist-checkin    # еженедельно
```

**Прямое обращение** к агенту: `@director`, `@strategist`, `@demiurg` или к подключённому.

## Layered memory

У каждого агента — 3 файла:
- `core.md` — ядро, обновляется из template при будущих апдейтах
- `overrides.md` — твои персональные правила, приоритет над core, **не перезаписывается** при апдейтах
- `CLAUDE.md` — склейка `@core.md` + `@overrides.md` (Claude Code читает это)

Пиши свои кастомизации в `overrides.md`. Так ты не потеряешь их когда обновится ядро.
