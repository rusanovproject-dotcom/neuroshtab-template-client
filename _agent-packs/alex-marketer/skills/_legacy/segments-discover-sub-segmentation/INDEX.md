# segments-discover-sub-segmentation — INDEX

Под-скилл сегментации для `/segments-discover`. Используется как методологический референс.

## Файлы

- `SKILL.md` — основной протокол
- `references/` — методологические выжимки (если есть)

## Использование

Не вызывается напрямую. Подгружается родительским `/segments-discover` Этап 4 для генерации 12 гипотез по 3W.
